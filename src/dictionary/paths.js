import w from './words';


const paths = {
    register: `/${w.register}`,
    login: `/${w.login}`,
    dashboard: '/',
    createCard: `${w.create}-${w.card}`,
    inventory: `${w.inventory}`
}

export default paths;
